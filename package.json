{
  "name": "blog-website",
  "version": "1.0.0",
  "description": "Responsive blog website",
  "scripts": {
    "start": "npx live-server",
    "dev": "live-server --port=3000"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
