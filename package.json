{
  "name": "blog-website",
  "version": "1.0.0",
  "description": "Responsive blog website",
  "scripts": {
    "start": "live-server",
    "dev": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
